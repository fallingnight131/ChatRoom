package com.fallingnight.chat.application.notification;

/** Fixed-cardinality subscription mutation actions for admission and telemetry. */
public enum WebPushSubscriptionMutationAction {
    REPLACE,
    DELETE
}

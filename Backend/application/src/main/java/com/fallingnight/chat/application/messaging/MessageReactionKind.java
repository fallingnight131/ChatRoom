package com.fallingnight.chat.application.messaging;

/** Stable application identities for the bounded V2 reaction set. */
public enum MessageReactionKind {
    LIKE,
    LOVE,
    LAUGH,
    SURPRISED,
    SAD,
    ANGRY
}
